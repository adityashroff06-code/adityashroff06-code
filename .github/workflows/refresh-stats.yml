name: Refresh profile stats

# Rebuilds the "GitHub pulse" card and the contribution snake from the GitHub API
# and publishes them to the `output` branch, which README.md reads from.
# Uses only the built-in GITHUB_TOKEN; no personal token or third-party stats service.
on:
  push:
    branches: [main]
  schedule:
    - cron: "23 1 * * *"   # daily at 01:23 UTC
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: profile-stats
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - name: Render GitHub pulse card
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_USER: ${{ github.repository_owner }}
        run: python3 scripts/build_stats.py --user "$GH_USER" --out dist

      - name: Render contribution snake
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/snake-dark.svg?palette=github-dark&color_snake=#22D3EE&color_dots=#161B2E,#2E2766,#4C3AA8,#7C5CF0,#B39DFF
            dist/snake-light.svg?palette=github-light&color_snake=#7C3AED&color_dots=#EBEDF0,#DDD6FE,#C4B5FD,#A78BFA,#6D28D9

      - name: Publish to the output branch
        uses: crazy-max/ghaction-github-pages@v5
        with:
          target_branch: output
          build_dir: dist
          jekyll: false
          commit_message: Refresh profile stats
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
